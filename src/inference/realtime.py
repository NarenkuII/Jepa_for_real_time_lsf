from __future__ import annotations


def realtime_loop(config: dict, checkpoint: str | None = None) -> None:
    raise RuntimeError("Realtime inference requires a trained checkpoint plus optional vision dependencies.")

